md-card.border-round(flex layout-padding)
  form(name="loginForm" ng-submit="login()" novalidate)
    .container(md-block flex layout="row")
      div(flex=10)
        i.material-icons.md-24.icon-padding-top account_box
      div(flex=90 layout="column")
        md-input-container(md-block)
          label Username
          input(type="text" name="username" ng-model="user.username" ng-required="true" ng-minlength="4" ng-pattern="/^[\w_.]+/" ng-model-options="{updateOn: 'mousedown',  debounce: 200}")
        div(flex md-block)
          span.error(ng-show="loginForm.username.$dirty && loginForm.username.error.$required") Please provide your account's username

    .container(md-block flex layout="row")
      div(flex=10)
        i.material-icons.md-24.icon-padding-top lock
      div(flex=90 layout="column")
        md-input-container(md-block)
          label Password
          input(type="password" name="password" ng-model="user.password" ng-required="true" ng-minlength="6" ng-model-options="{updateOn: 'mousedown',  debounce: 200}")
        div(flex md-block)
          span.error(ng-show="loginForm.password.$dirty && loginForm.password.error.$required") A password is required to log you in

    .container(md-block flex layout="row")
      button(flex type="submit").md-button.md-accent.md-raised.md-hue-2 Log in
